openapi: 3.0.3
info:
  title: Last Mile Delivery Platform API
  description: |
    API REST para la plataforma SaaS de last mile delivery multi-pais.
    Multi-tenant con autenticacion JWT, facturacion fiscal multi-pais (MX/BR/CO/AR/CL/PE/UY/EC),
    pagos Stripe/MercadoPago, i18n (ES/EN/PT), notificaciones multi-idioma, PWA chofer.
  version: 4.0.0
  contact:
    name: Last Mile Support
    email: soporte@lastmile.mx
  license:
    name: Proprietary

servers:
  - url: https://lastmile-platform.onrender.com
    description: Production
  - url: http://localhost:8080
    description: Development

tags:
  - name: Auth
    description: Autenticacion y tokens
  - name: Health
    description: Health checks
  - name: Pedidos
    description: Gestion de pedidos/envios
  - name: Zonas
    description: Zonas y tarifas
  - name: Choferes
    description: Gestion de choferes
  - name: Vehiculos
    description: Gestion de vehiculos
  - name: Clientes
    description: Clientes del tenant
  - name: Billing
    description: Facturacion SaaS y planes
  - name: CFDI
    description: Facturacion electronica CFDI 4.0
  - name: Fiscal
    description: Sistema fiscal multi-pais (MX, BR, CO, AR, CL, PE, UY, EC)
  - name: Pagos
    description: Pagos y transacciones multi-pais
  - name: Webhooks
    description: Webhooks de Stripe y MercadoPago
  - name: Usuarios
    description: Gestion de usuarios del tenant
  - name: SuperAdmin
    description: Gestion global de tenants (solo superadmin)
  - name: Tracking
    description: Tracking publico para clientes finales
  - name: Referidos
    description: Sistema de referidos

paths:
  /api/health:
    get:
      tags: [Health]
      summary: Health check del sistema
      security: []
      responses:
        '200':
          description: Sistema operativo
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HealthResponse'

  /api/auth/login:
    post:
      tags: [Auth]
      summary: Iniciar sesion
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [user, pass]
              properties:
                user:
                  type: string
                  example: admin
                pass:
                  type: string
                  example: admin123
                emp_id:
                  type: integer
                  description: ID de empresa (opcional, para multi-tenant)
      responses:
        '200':
          description: Login exitoso
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/LoginResponse'

  /api/auth/refresh:
    post:
      tags: [Auth]
      summary: Refrescar access token
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [refresh_token]
              properties:
                refresh_token:
                  type: string
      responses:
        '200':
          description: Nuevo access token
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                  token:
                    type: string
                  expires_in:
                    type: integer
        '401':
          description: Refresh token invalido

  /api/pedidos:
    get:
      tags: [Pedidos]
      summary: Listar pedidos del tenant
      security:
        - bearerAuth: []
      parameters:
        - name: limite
          in: query
          schema:
            type: integer
            default: 100
        - name: estado
          in: query
          schema:
            type: string
            enum: [PENDIENTE, ASIGNADO, RECOGIENDO, EN_RUTA, ENTREGADO, CANCELADO]
      responses:
        '200':
          description: Lista de pedidos
    post:
      tags: [Pedidos]
      summary: Crear un pedido/envio
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PedidoCreate'
      responses:
        '200':
          description: Pedido creado

  /api/pedidos/{ped_id}:
    delete:
      tags: [Pedidos]
      summary: Eliminar un pedido
      security:
        - bearerAuth: []
      parameters:
        - name: ped_id
          in: path
          required: true
          schema:
            type: integer
      responses:
        '200':
          description: Pedido eliminado

  /api/zonas:
    get:
      tags: [Zonas]
      summary: Listar zonas del tenant
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Lista de zonas
    post:
      tags: [Zonas]
      summary: Crear zona
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                nombre:
                  type: string
                descripcion:
                  type: string
                coordenadas:
                  type: string
      responses:
        '200':
          description: Zona creada

  /api/zonas/cotizar:
    post:
      tags: [Zonas]
      summary: Cotizar envio por zona
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                zona_id:
                  type: integer
                servicio:
                  type: string
                  enum: [ESTANDAR, EXPRESS, SAME_DAY]
                peso_kg:
                  type: number
                distancia_km:
                  type: number
                valor_declarado:
                  type: number
      responses:
        '200':
          description: Cotizacion

  /api/choferes:
    get:
      tags: [Choferes]
      summary: Listar choferes
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Lista de choferes
    post:
      tags: [Choferes]
      summary: Crear chofer
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ChoferCreate'
      responses:
        '200':
          description: Chofer creado

  /api/choferes/{cho_id}:
    delete:
      tags: [Choferes]
      summary: Eliminar chofer
      security:
        - bearerAuth: []
      parameters:
        - name: cho_id
          in: path
          required: true
          schema:
            type: integer
      responses:
        '200':
          description: Chofer eliminado

  /api/vehiculos:
    get:
      tags: [Vehiculos]
      summary: Listar vehiculos
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Lista de vehiculos
    post:
      tags: [Vehiculos]
      summary: Crear vehiculo
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/VehiculoCreate'
      responses:
        '200':
          description: Vehiculo creado

  /api/vehiculos/{veh_id}:
    delete:
      tags: [Vehiculos]
      summary: Eliminar vehiculo
      security:
        - bearerAuth: []
      parameters:
        - name: veh_id
          in: path
          required: true
          schema:
            type: integer
      responses:
        '200':
          description: Vehiculo eliminado

  /api/clientes:
    get:
      tags: [Clientes]
      summary: Listar clientes
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Lista de clientes
    post:
      tags: [Clientes]
      summary: Crear cliente
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ClienteCreate'
      responses:
        '200':
          description: Cliente creado

  /api/clientes/{cli_id}:
    delete:
      tags: [Clientes]
      summary: Eliminar cliente
      security:
        - bearerAuth: []
      parameters:
        - name: cli_id
          in: path
          required: true
          schema:
            type: integer
      responses:
        '200':
          description: Cliente eliminado

  /api/usuarios:
    get:
      tags: [Usuarios]
      summary: Listar usuarios del tenant
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Lista de usuarios

  /api/usuarios/{usu_id}:
    delete:
      tags: [Usuarios]
      summary: Eliminar usuario
      security:
        - bearerAuth: []
      parameters:
        - name: usu_id
          in: path
          required: true
          schema:
            type: integer
      responses:
        '200':
          description: Usuario eliminado

  /api/billing/planes:
    get:
      tags: [Billing]
      summary: Listar planes disponibles
      security: []
      responses:
        '200':
          description: Lista de planes

  /api/billing/mis-limites:
    get:
      tags: [Billing]
      summary: Ver limites del plan actual
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Limites del tenant

  /api/billing/stats:
    get:
      tags: [Billing]
      summary: Estadisticas de uso del tenant
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Estadisticas

  /api/billing/webhook/stripe:
    post:
      tags: [Billing]
      summary: Webhook de Stripe (verificado por firma)
      security: []
      responses:
        '200':
          description: Webhook procesado

  /api/cfdi/status:
    get:
      tags: [CFDI]
      summary: Estado del servicio de facturacion
      security: []
      responses:
        '200':
          description: Estado CFDI

  /api/cfdi/facturas:
    get:
      tags: [CFDI]
      summary: Listar facturas emitidas
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Lista de facturas

  /api/cfdi/facturas/{fac_id}/cancelar:
    post:
      tags: [CFDI]
      summary: Cancelar factura
      security:
        - bearerAuth: []
      parameters:
        - name: fac_id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                motivo:
                  type: string
      responses:
        '200':
          description: Factura cancelada

  /api/pagos/transacciones:
    get:
      tags: [Pagos]
      summary: Listar transacciones de pago
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Lista de transacciones

  /api/pagos/transacciones/{pag_id}:
    delete:
      tags: [Pagos]
      summary: Eliminar transaccion
      security:
        - bearerAuth: []
      parameters:
        - name: pag_id
          in: path
          required: true
          schema:
            type: integer
      responses:
        '200':
          description: Transaccion eliminada

  /api/billing/cancelar:
    post:
      tags: [Billing]
      summary: Cancelar suscripcion SaaS
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Suscripcion cancelada

  /api/onboarding/register:
    post:
      tags: [Auth]
      summary: Registrar nuevo tenant con admin
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RegisterRequest'
      responses:
        '200':
          description: Tenant registrado

  /api/saas/tenants:
    get:
      tags: [SuperAdmin]
      summary: Listar todos los tenants (solo superadmin)
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Lista de tenants

  /api/saas/tenants/{emp_id}/suspend:
    post:
      tags: [SuperAdmin]
      summary: Suspender tenant (solo superadmin)
      security:
        - bearerAuth: []
      parameters:
        - name: emp_id
          in: path
          required: true
          schema:
            type: integer
      responses:
        '200':
          description: Tenant suspendido

  /api/saas/tenants/{emp_id}/activate:
    post:
      tags: [SuperAdmin]
      summary: Activar tenant (solo superadmin)
      security:
        - bearerAuth: []
      parameters:
        - name: emp_id
          in: path
          required: true
          schema:
            type: integer
      responses:
        '200':
          description: Tenant activado

  /api/tracking/{token}:
    get:
      tags: [Tracking]
      summary: Obtener estado del pedido por token de tracking
      security: []
      parameters:
        - name: token
          in: path
          required: true
          schema:
            type: string
      responses:
        '200':
          description: Estado del pedido

  # ===== MULTI-COUNTRY FISCAL =====
  /api/fiscal/countries:
    get:
      tags: [Fiscal]
      summary: Listar paises con soporte fiscal
      security: [{bearerAuth: []}]
      responses:
        '200':
          description: Lista de 8 paises (MX, BR, CO, AR, CL, PE, UY, EC)

  /api/fiscal/providers:
    get:
      tags: [Fiscal]
      summary: Listar proveedores fiscales disponibles
      security: [{bearerAuth: []}]
      responses:
        '200':
          description: Proveedores con monedas

  /api/fiscal/config:
    get:
      tags: [Fiscal]
      summary: Obtener configuracion fiscal del tenant
      security: [{bearerAuth: []}]
      responses:
        '200':
          description: Configuracion fiscal
    put:
      tags: [Fiscal]
      summary: Guardar configuracion fiscal del tenant
      security: [{bearerAuth: []}]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [country_code]
              properties:
                country_code:
                  type: string
                  enum: [MX, BR, CO, AR, CL, PE, UY, EC]
                api_key:
                  type: string
                api_secret:
                  type: string
                base_url:
                  type: string
      responses:
        '200':
          description: Configuracion guardada

  /api/fiscal/test-connection:
    post:
      tags: [Fiscal]
      summary: Probar conexion con proveedor fiscal
      security: [{bearerAuth: []}]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                country_code:
                  type: string
      responses:
        '200':
          description: Resultado de prueba de conexion

  /api/fiscal/emit:
    post:
      tags: [Fiscal]
      summary: Emitir documento fiscal
      security: [{bearerAuth: []}]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
      responses:
        '200':
          description: Documento emitido

  /api/fiscal/cancel:
    post:
      tags: [Fiscal]
      summary: Cancelar documento fiscal
      security: [{bearerAuth: []}]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                document_id:
                  type: string
                reason:
                  type: string
      responses:
        '200':
          description: Documento cancelado

  /api/system/migrate:
    post:
      tags: [Fiscal]
      summary: Ejecutar migracion de tablas fiscales (admin)
      security: [{bearerAuth: []}]
      responses:
        '200':
          description: Migracion ejecutada

  # ===== MULTI-COUNTRY PAYMENTS =====
  /api/payment/countries:
    get:
      tags: [Pagos]
      summary: Listar paises con metodos de pago
      security: [{bearerAuth: []}]
      responses:
        '200':
          description: Lista de paises con pagos

  /api/payment/methods/{country_code}:
    get:
      tags: [Pagos]
      summary: Metodos de pago por pais
      security: [{bearerAuth: []}]
      parameters:
        - name: country_code
          in: path
          required: true
          schema:
            type: string
      responses:
        '200':
          description: Metodos de pago del pais

  /api/payments/create:
    post:
      tags: [Pagos]
      summary: Crear pago multi-pais
      security: [{bearerAuth: []}]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [amount, country_code]
              properties:
                amount:
                  type: number
                country_code:
                  type: string
                payment_method:
                  type: string
                description:
                  type: string
                order_id:
                  type: string
      responses:
        '200':
          description: Sesion de pago creada

  /api/payments/status/{provider}/{payment_id}:
    get:
      tags: [Pagos]
      summary: Consultar estado de pago
      security: [{bearerAuth: []}]
      parameters:
        - name: provider
          in: path
          required: true
          schema:
            type: string
            enum: [stripe, mercadopago]
        - name: payment_id
          in: path
          required: true
          schema:
            type: string
      responses:
        '200':
          description: Estado del pago

  /api/payments/refund:
    post:
      tags: [Pagos]
      summary: Reembolsar pago
      security: [{bearerAuth: []}]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [provider, payment_id]
              properties:
                provider:
                  type: string
                payment_id:
                  type: string
                amount:
                  type: number
      responses:
        '200':
          description: Reembolso procesado

  /api/payments/status-summary:
    get:
      tags: [Pagos]
      summary: Estado de configuracion de proveedores de pago
      security: [{bearerAuth: []}]
      responses:
        '200':
          description: Resumen de estado

  # ===== WEBHOOKS (PUBLIC - NO AUTH) =====
  /api/webhooks/stripe:
    post:
      tags: [Webhooks]
      summary: Webhook de Stripe (pagos, reembolsos, etc)
      security: []
      responses:
        '200':
          description: Evento procesado

  /api/webhooks/mercadopago:
    post:
      tags: [Webhooks]
      summary: Webhook de MercadoPago (pagos, etc)
      security: []
      responses:
        '200':
          description: Evento procesado

  # ===== ANALYTICS =====
  /api/analytics/multi-country:
    get:
      tags: [Fiscal]
      summary: Analytics multi-pais (fiscal, pagos, ordenes)
      security: [{bearerAuth: []}]
      responses:
        '200':
          description: Datos analiticos multi-pais
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                  data:
                    type: object
                    properties:
                      countries_supported:
                        type: array
                        items:
                          type: string
                      fiscal_configured:
                        type: array
                        items:
                          type: object
                          properties:
                            country:
                              type: string
                            tenants:
                              type: integer
                      payment_methods:
                        type: array
                        items:
                          type: object
                          properties:
                            country:
                              type: string
                            methods:
                              type: integer
                      total_tenants:
                        type: integer
                      orders_by_status:
                        type: array

  # ===== REPORTES =====
  /api/reportes:
    get:
      tags: [Pedidos]
      summary: Reportes disponibles
      security: [{bearerAuth: []}]
      responses:
        '200':
          description: Lista de reportes

  /api/reportes/generar:
    post:
      tags: [Pedidos]
      summary: Generar reporte personalizado
      security: [{bearerAuth: []}]
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                tipo:
                  type: string
                  enum: [entregas, financiero, rendimiento, costos]
                fecha_inicio:
                  type: string
                  format: date
                fecha_fin:
                  type: string
                  format: date
      responses:
        '200':
          description: Reporte generado

  /api/reportes/entregas:
    get:
      tags: [Pedidos]
      summary: Reporte de entregas
      security: [{bearerAuth: []}]
      responses:
        '200':
          description: Datos de entregas

  /api/reportes/rendimiento-choferes:
    get:
      tags: [Choferes]
      summary: Reporte de rendimiento de choferes
      security: [{bearerAuth: []}]
      responses:
        '200':
          description: Rendimiento por chofer

  /api/reportes/costos-rutas:
    get:
      tags: [Pedidos]
      summary: Reporte de costos por ruta
      security: [{bearerAuth: []}]
      responses:
        '200':
          description: Analisis de costos

  # ===== NOTIFICACIONES =====
  /api/notifications/send:
    post:
      tags: [Usuarios]
      summary: Enviar notificacion (email/SMS)
      security: [{bearerAuth: []}]
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                template:
                  type: string
                to:
                  type: string
      responses:
        '200':
          description: Notificacion enviada

  /api/notifications/custom:
    post:
      tags: [Usuarios]
      summary: Enviar notificacion personalizada
      security: [{bearerAuth: []}]
      responses:
        '200':
          description: Notificacion enviada

  /api/notifications/test:
    post:
      tags: [Usuarios]
      summary: Enviar notificacion de prueba
      security: [{bearerAuth: []}]
      responses:
        '200':
          description: Prueba enviada

  /api/notifications/config:
    get:
      tags: [Usuarios]
      summary: Configuracion de notificaciones
      security: [{bearerAuth: []}]
      responses:
        '200':
          description: Configuracion actual

  # ===== MANTENIMIENTO =====
  /api/mantenimiento/unidades:
    get:
      tags: [Vehiculos]
      summary: Unidades de mantenimiento
      security: [{bearerAuth: []}]
      responses:
        '200':
          description: Lista de unidades

  /api/mantenimiento/ots:
    get:
      tags: [Vehiculos]
      summary: Ordenes de trabajo de mantenimiento
      security: [{bearerAuth: []}]
      responses:
        '200':
          description: Lista de OTs

  # ===== SAAS ADMIN =====
  /api/saas/global-stats:
    get:
      tags: [SuperAdmin]
      summary: Estadisticas globales SaaS
      security: [{bearerAuth: []}]
      responses:
        '200':
          description: Stats globales

  /api/saas/cobros:
    get:
      tags: [SuperAdmin]
      summary: Cobros y pagos globales
      security: [{bearerAuth: []}]
      responses:
        '200':
          description: Lista de cobros

  /api/saas/cobros/resumen:
    get:
      tags: [SuperAdmin]
      summary: Resumen de cobros
      security: [{bearerAuth: []}]
      responses:
        '200':
          description: Resumen

  /api/saas/suscripciones:
    get:
      tags: [SuperAdmin]
      summary: Suscripciones globales
      security: [{bearerAuth: []}]
      responses:
        '200':
          description: Lista de suscripciones

  /api/saas/audit:
    get:
      tags: [SuperAdmin]
      summary: Auditoria global
      security: [{bearerAuth: []}]
      responses:
        '200':
          description: Log de auditoria

  /api/saas/config:
    get:
      tags: [SuperAdmin]
      summary: Configuracion SaaS
      security: [{bearerAuth: []}]
      responses:
        '200':
          description: Configuracion

  /api/saas/planes:
    get:
      tags: [SuperAdmin]
      summary: Planes SaaS
      security: [{bearerAuth: []}]
      responses:
        '200':
          description: Lista de planes

  /api/saas/revenue-chart:
    get:
      tags: [SuperAdmin]
      summary: Grafica de revenue
      security: [{bearerAuth: []}]
      responses:
        '200':
          description: Datos de revenue

  /api/saas/all-pedidos:
    get:
      tags: [SuperAdmin]
      summary: Todos los pedidos globales
      security: [{bearerAuth: []}]
      responses:
        '200':
          description: Lista de pedidos

  /api/saas/all-users:
    get:
      tags: [SuperAdmin]
      summary: Todos los usuarios globales
      security: [{bearerAuth: []}]
      responses:
        '200':
          description: Lista de usuarios

  # ===== EMPRESAS =====
  /api/empresas:
    get:
      tags: [Pedidos]
      summary: Listar todas las empresas
      security: [{bearerAuth: []}]
      responses:
        '200':
          description: Lista de empresas

  /api/empresas/{emp_id}:
    get:
      tags: [Pedidos]
      summary: Obtener empresa por ID
      security: [{bearerAuth: []}]
      parameters:
        - name: emp_id
          in: path
          required: true
          schema:
            type: integer
      responses:
        '200':
          description: Empresa encontrada

  # ===== PEDIDOS DETALLE =====
  /api/pedidos/{ped_id}/asignar:
    post:
      tags: [Pedidos]
      summary: Asignar chofer a pedido
      security: [{bearerAuth: []}]
      parameters:
        - name: ped_id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                cho_id:
                  type: integer
                veh_id:
                  type: integer
                notas:
                  type: string
      responses:
        '200':
          description: Chofer asignado

  /api/pedidos/{ped_id}/estado:
    put:
      tags: [Pedidos]
      summary: Actualizar estado del pedido
      security: [{bearerAuth: []}]
      parameters:
        - name: ped_id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        content:
          application/json:
            schema:
              type: object
              required: [estado]
              properties:
                estado:
                  type: string
                  enum: [PENDIENTE, ASIGNADO, RECOGIENDO, EN_RUTA, ENTREGADO, CANCELADO]
                usuario:
                  type: string
      responses:
        '200':
          description: Estado actualizado

  /api/pedidos/estadisticas:
    get:
      tags: [Pedidos]
      summary: Estadisticas de pedidos del tenant
      security: [{bearerAuth: []}]
      responses:
        '200':
          description: Estadisticas

  # ===== DASHBOARD =====
  /api/dashboard/{emp_id}:
    get:
      tags: [Health]
      summary: Dashboard de resumen del tenant
      security: [{bearerAuth: []}]
      parameters:
        - name: emp_id
          in: path
          required: true
          schema:
            type: integer
      responses:
        '200':
          description: Datos del dashboard

  /api/dashboard/{emp_id}/charts:
    get:
      tags: [Health]
      summary: Datos de graficas del dashboard
      security: [{bearerAuth: []}]
      parameters:
        - name: emp_id
          in: path
          required: true
          schema:
            type: integer
      responses:
        '200':
          description: Datos de graficas

  # ===== CHOFERES DETALLE =====
  /api/choferes/rendimiento:
    get:
      tags: [Choferes]
      summary: Rendimiento de choferes (paginado)
      security: [{bearerAuth: []}]
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            default: 50
        - name: offset
          in: query
          schema:
            type: integer
            default: 0
      responses:
        '200':
          description: Rendimiento con paginacion

  # ===== VEHICULOS DETALLE =====
  /api/vehiculos/flota:
    get:
      tags: [Vehiculos]
      summary: Estado de la flota
      security: [{bearerAuth: []}]
      responses:
        '200':
          description: Estado de flota

  # ===== CLIENTES DETALLE =====
  /api/clientes/top:
    get:
      tags: [Clientes]
      summary: Top clientes por gasto
      security: [{bearerAuth: []}]
      responses:
        '200':
          description: Lista de top clientes

  # ===== USUARIOS DETALLE =====
  /api/usuarios/{usu_id}:
    get:
      tags: [Usuarios]
      summary: Obtener usuario por ID
      security: [{bearerAuth: []}]
      parameters:
        - name: usu_id
          in: path
          required: true
          schema:
            type: integer
      responses:
        '200':
          description: Usuario encontrado
    put:
      tags: [Usuarios]
      summary: Actualizar usuario
      security: [{bearerAuth: []}]
      parameters:
        - name: usu_id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UsuarioUpdate'
      responses:
        '200':
          description: Usuario actualizado

  # ===== RUTAS / ENTREGAS =====
  /api/rutas:
    get:
      tags: [Pedidos]
      summary: Costos por ruta
      security: [{bearerAuth: []}]
      responses:
        '200':
          description: Lista de rutas con costos

  /api/entregas:
    get:
      tags: [Pedidos]
      summary: Historial de entregas (paginado)
      security: [{bearerAuth: []}]
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            default: 50
        - name: offset
          in: query
          schema:
            type: integer
            default: 0
      responses:
        '200':
          description: Lista de entregas

  /api/entregas/chofer/{cho_id}:
    get:
      tags: [Choferes]
      summary: Entregas por chofer
      security: [{bearerAuth: []}]
      parameters:
        - name: cho_id
          in: path
          required: true
          schema:
            type: integer
      responses:
        '200':
          description: Entregas del chofer

  # ===== KPIs =====
  /api/kpis:
    get:
      tags: [Health]
      summary: KPIs consolidados del tenant
      security: [{bearerAuth: []}]
      responses:
        '200':
          description: KPIs

  /api/kpis/diario:
    get:
      tags: [Health]
      summary: KPIs diarios (ultimos 30 dias)
      security: [{bearerAuth: []}]
      responses:
        '200':
          description: KPIs diarios

  # ===== INCIDENCIAS =====
  /api/incidencias:
    get:
      tags: [Pedidos]
      summary: Lista de incidencias
      security: [{bearerAuth: []}]
      responses:
        '200':
          description: Lista de incidencias

  /api/incidencias/resumen:
    get:
      tags: [Pedidos]
      summary: Resumen de incidencias
      security: [{bearerAuth: []}]
      responses:
        '200':
          description: Resumen

  # ===== TRACKING DETALLE =====
  /api/tracking:
    post:
      tags: [Tracking]
      summary: Registrar posicion GPS
      security: [{bearerAuth: []}]
      requestBody:
        content:
          application/json:
            schema:
              type: object
              required: [choId, latitud, longitud]
              properties:
                choId:
                  type: integer
                vehId:
                  type: integer
                latitud:
                  type: number
                longitud:
                  type: number
                velocidad:
                  type: number
                rumbo:
                  type: number
                bateria:
                  type: integer
      responses:
        '200':
          description: Tracking registrado

  /api/tracking/live:
    get:
      tags: [Tracking]
      summary: Posicion en tiempo real de todos los choferes
      security: [{bearerAuth: []}]
      responses:
        '200':
          description: Posiciones actuales

  /api/tracking/route/{cho_id}:
    get:
      tags: [Tracking]
      summary: Historial de ruta de un chofer (24h)
      security: [{bearerAuth: []}]
      parameters:
        - name: cho_id
          in: path
          required: true
          schema:
            type: integer
      responses:
        '200':
          description: Historial de ruta

  /api/tracking/{token}:
    get:
      tags: [Tracking]
      summary: Tracking publico del cliente final por token
      security: []
      parameters:
        - name: token
          in: path
          required: true
          schema:
            type: string
      responses:
        '200':
          description: Estado del pedido

  # ===== AUDITORIA =====
  /api/audit:
    get:
      tags: [SuperAdmin]
      summary: Log de auditoria del tenant
      security: [{bearerAuth: []}]
      responses:
        '200':
          description: Registros de auditoria

  # ===== CFDI DETALLE =====
  /api/cfdi/facturas/{fac_id}/timbrar:
    post:
      tags: [CFDI]
      summary: Timbrar factura CFDI
      security: [{bearerAuth: []}]
      parameters:
        - name: fac_id
          in: path
          required: true
          schema:
            type: integer
      responses:
        '200':
          description: Factura timbrada

  /api/cfdi/catalogo:
    get:
      tags: [CFDI]
      summary: Catalogo de conceptos CFDI
      security: [{bearerAuth: []}]
      responses:
        '200':
          description: Catalogo

  /api/cfdi/empresa-fiscal:
    get:
      tags: [CFDI]
      summary: Datos fiscales de la empresa
      security: [{bearerAuth: []}]
      responses:
        '200':
          description: Datos fiscales
    put:
      tags: [CFDI]
      summary: Actualizar datos fiscales de la empresa
      security: [{bearerAuth: []}]
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                rfc:
                  type: string
                razonSocial:
                  type: string
                regimenFiscal:
                  type: string
                codigoPostal:
                  type: string
      responses:
        '200':
          description: Datos actualizados

  /api/cfdi/folios:
    get:
      tags: [CFDI]
      summary: Folios disponibles
      security: [{bearerAuth: []}]
      responses:
        '200':
          description: Lista de folios

  /api/cfdi/timbrado-log:
    get:
      tags: [CFDI]
      summary: Log de timbrado
      security: [{bearerAuth: []}]
      responses:
        '200':
          description: Log de timbrado

  # ===== PAGOS DETALLE =====
  /api/pagos/metodos:
    get:
      tags: [Pagos]
      summary: Metodos de pago configurados
      security: [{bearerAuth: []}]
      responses:
        '200':
          description: Metodos de pago

  /api/pagos/resumen:
    get:
      tags: [Pagos]
      summary: Resumen de pagos por metodo
      security: [{bearerAuth: []}]
      responses:
        '200':
          description: Resumen

  /api/pagos/oxxo:
    post:
      tags: [Pagos]
      summary: Crear referencia de pago OXXO
      security: [{bearerAuth: []}]
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                pedId:
                  type: integer
                monto:
                  type: number
      responses:
        '200':
          description: Referencia OXXO generada

  # ===== WHITELABEL =====
  /api/whitelabel/{emp_id}:
    get:
      tags: [SuperAdmin]
      summary: Configuracion whitelabel del tenant
      security: [{bearerAuth: []}]
      parameters:
        - name: emp_id
          in: path
          required: true
          schema:
            type: integer
      responses:
        '200':
          description: Config whitelabel

  /api/whitelabel/config:
    post:
      tags: [SuperAdmin]
      summary: Actualizar configuracion whitelabel
      security: [{bearerAuth: []}]
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                nombre:
                  type: string
                logo_url:
                  type: string
                color_primary:
                  type: string
                color_secondary:
                  type: string
      responses:
        '200':
          description: Whitelabel actualizado

  # ===== NOTIFICACIONES DETALLE =====
  /api/notif/push:
    get:
      tags: [Usuarios]
      summary: Notificaciones push del tenant
      security: [{bearerAuth: []}]
      responses:
        '200':
          description: Lista de notificaciones
    post:
      tags: [Usuarios]
      summary: Enviar notificacion push
      security: [{bearerAuth: []}]
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                tipo:
                  type: string
                titulo:
                  type: string
                cuerpo:
                  type: string
      responses:
        '200':
          description: Notificacion enviada

  /api/notif/dispositivos:
    post:
      tags: [Usuarios]
      summary: Registrar dispositivo push
      security: [{bearerAuth: []}]
      responses:
        '200':
          description: Dispositivo registrado

  /api/notif/stats:
    get:
      tags: [Usuarios]
      summary: Estadisticas de notificaciones
      security: [{bearerAuth: []}]
      responses:
        '200':
          description: Estadisticas

  # ===== EMAIL / SMS =====
  /api/email/enviados:
    get:
      tags: [Usuarios]
      summary: Emails enviados
      security: [{bearerAuth: []}]
      responses:
        '200':
          description: Lista de emails

  /api/email/enviar:
    post:
      tags: [Usuarios]
      summary: Enviar email
      security: [{bearerAuth: []}]
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                destinatario:
                  type: string
                asunto:
                  type: string
                bodyHtml:
                  type: string
      responses:
        '200':
          description: Email enviado

  /api/email/stats:
    get:
      tags: [Usuarios]
      summary: Estadisticas de emails
      security: [{bearerAuth: []}]
      responses:
        '200':
          description: Estadisticas

  /api/sms/enviados:
    get:
      tags: [Usuarios]
      summary: SMS enviados
      security: [{bearerAuth: []}]
      responses:
        '200':
          description: Lista de SMS

  /api/sms/enviar:
    post:
      tags: [Usuarios]
      summary: Enviar SMS
      security: [{bearerAuth: []}]
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                telefono:
                  type: string
                mensaje:
                  type: string
      responses:
        '200':
          description: SMS enviado

  /api/sms/stats:
    get:
      tags: [Usuarios]
      summary: Estadisticas de SMS
      security: [{bearerAuth: []}]
      responses:
        '200':
          description: Estadisticas

  # ===== EXPORTACION =====
  /api/export/{entity}:
    get:
      tags: [Pedidos]
      summary: Exportar entidad como CSV/PDF
      security: [{bearerAuth: []}]
      parameters:
        - name: entity
          in: path
          required: true
          schema:
            type: string
            enum: [pedidos, clientes, choferes, vehiculos, usuarios, pagos, facturas]
        - name: format
          in: query
          schema:
            type: string
            enum: [csv, pdf]
            default: csv
      responses:
        '200':
          description: Archivo exportado

  /api/export/custom:
    post:
      tags: [Pedidos]
      summary: Exportar reporte personalizado
      security: [{bearerAuth: []}]
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                report_type:
                  type: string
                format:
                  type: string
                  enum: [csv, pdf]
      responses:
        '200':
          description: Reporte exportado

  # ===== BILLING DETALLE =====
  /api/billing/estado:
    get:
      tags: [Billing]
      summary: Estado de facturacion del tenant
      security: [{bearerAuth: []}]
      responses:
        '200':
          description: Estado de billing

  /api/billing/checkout:
    post:
      tags: [Billing]
      summary: Crear sesion de checkout (Stripe/MercadoPago)
      security: [{bearerAuth: []}]
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                plan:
                  type: string
                provider:
                  type: string
                  enum: [stripe, mercadopago]
      responses:
        '200':
          description: Sesion de checkout

  /api/billing/suscripcion:
    post:
      tags: [Billing]
      summary: Activar suscripcion manual
      security: [{bearerAuth: []}]
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                plan:
                  type: string
      responses:
        '200':
          description: Suscripcion activada

  /api/billing/pagos:
    get:
      tags: [Billing]
      summary: Historial de pagos del tenant
      security: [{bearerAuth: []}]
      responses:
        '200':
          description: Lista de pagos

  /api/billing/pago:
    post:
      tags: [Billing]
      summary: Registrar un pago
      security: [{bearerAuth: []}]
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                monto:
                  type: number
                metodo:
                  type: string
                referencia:
                  type: string
      responses:
        '200':
          description: Pago registrado

  /api/billing/stats-all:
    get:
      tags: [Billing]
      summary: Estadisticas de billing global
      security: [{bearerAuth: []}]
      responses:
        '200':
          description: Estadisticas globales

  # ===== CLIENTE FINAL =====
  /api/cliente-final:
    post:
      tags: [Tracking]
      summary: Generar token de tracking para cliente
      security: [{bearerAuth: []}]
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                pedId:
                  type: integer
                nombre:
                  type: string
                telefono:
                  type: string
                email:
                  type: string
      responses:
        '200':
          description: Token generado

  /api/cliente-final/{token}/timeline:
    get:
      tags: [Tracking]
      summary: Timeline del pedido por token
      security: []
      parameters:
        - name: token
          in: path
          required: true
          schema:
            type: string
      responses:
        '200':
          description: Timeline del pedido

  # ===== SAASTAANTS DETALLE =====
  /api/saas/plan-usage/{emp_id}:
    get:
      tags: [SuperAdmin]
      summary: Uso del plan de un tenant
      security: [{bearerAuth: []}]
      parameters:
        - name: emp_id
          in: path
          required: true
          schema:
            type: integer
      responses:
        '200':
          description: Uso del plan

  /api/saas/health:
    get:
      tags: [SuperAdmin]
      summary: Health check del sistema SaaS
      security: []
      responses:
        '200':
          description: Estado del SaaS

  /api/saas/tenants/{emp_id}:
    get:
      tags: [SuperAdmin]
      summary: Detalle de un tenant
      security: [{bearerAuth: []}]
      parameters:
        - name: emp_id
          in: path
          required: true
          schema:
            type: integer
      responses:
        '200':
          description: Detalle del tenant
    put:
      tags: [SuperAdmin]
      summary: Actualizar tenant
      security: [{bearerAuth: []}]
      parameters:
        - name: emp_id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                nombre:
                  type: string
                estatus:
                  type: string
                plan:
                  type: string
      responses:
        '200':
          description: Tenant actualizado

  /api/saas/uso/{emp_id}:
    get:
      tags: [SuperAdmin]
      summary: Uso de recursos de un tenant
      security: [{bearerAuth: []}]
      parameters:
        - name: emp_id
          in: path
          required: true
          schema:
            type: integer
      responses:
        '200':
          description: Uso de recursos

  /api/saas/uso:
    post:
      tags: [SuperAdmin]
      summary: Registrar uso de recursos
      security: [{bearerAuth: []}]
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                pedidosCreados:
                  type: integer
                pedidosEntregados:
                  type: integer
      responses:
        '200':
          description: Uso registrado

  /api/saas/dashboard-billing:
    get:
      tags: [SuperAdmin]
      summary: Dashboard de billing SaaS
      security: [{bearerAuth: []}]
      responses:
        '200':
          description: Dashboard de billing

  /api/saas/tenants-chart:
    get:
      tags: [SuperAdmin]
      summary: Grafica de distribucion de tenants
      security: [{bearerAuth: []}]
      responses:
        '200':
          description: Datos de grafica

  # ===== VAPID / PUSH =====
  /api/vapid-public-key:
    get:
      tags: [Health]
      summary: Clave publica VAPID para push
      security: []
      responses:
        '200':
          description: Clave publica

  /api/push/subscribe:
    post:
      tags: [Health]
      summary: Suscribir dispositivo a push
      security: []
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                user_id:
                  type: string
                subscription:
                  type: object
      responses:
        '200':
          description: Suscripcion registrada

  # ===== CRON =====
  /api/cron/backup:
    post:
      tags: [Health]
      summary: Backup de la base de datos (cron)
      security: []
      parameters:
        - name: X-Cron-Key
          in: header
          required: true
          schema:
            type: string
      responses:
        '200':
          description: Backup generado

  /api/cron/backup/download:
    get:
      tags: [Health]
      summary: Descargar backup como JSON
      security: []
      parameters:
        - name: key
          in: query
          required: true
          schema:
            type: string
      responses:
        '200':
          description: Archivo JSON de backup

  # ===== ZONAS DETALLE =====
  /api/zonas/{zon_id}:
    get:
      tags: [Zonas]
      summary: Obtener zona por ID
      security: [{bearerAuth: []}]
      parameters:
        - name: zon_id
          in: path
          required: true
          schema:
            type: integer
      responses:
        '200':
          description: Zona encontrada
    put:
      tags: [Zonas]
      summary: Actualizar zona
      security: [{bearerAuth: []}]
      parameters:
        - name: zon_id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                nombre:
                  type: string
                descripcion:
                  type: string
      responses:
        '200':
          description: Zona actualizada
    delete:
      tags: [Zonas]
      summary: Eliminar zona
      security: [{bearerAuth: []}]
      parameters:
        - name: zon_id
          in: path
          required: true
          schema:
            type: integer
      responses:
        '200':
          description: Zona eliminada

  /api/tarifas:
    get:
      tags: [Zonas]
      summary: Listar todas las tarifas del tenant
      security: [{bearerAuth: []}]
      responses:
        '200':
          description: Lista de tarifas

  # ===== LEGAL =====
  /api/legal/my-acceptance:
    get:
      tags: [Usuarios]
      summary: Aceptacion legal del usuario actual
      security: [{bearerAuth: []}]
      responses:
        '200':
          description: Aceptaciones legales

  /api/legal/verify/{usr_id}:
    get:
      tags: [Usuarios]
      summary: Verificar aceptacion legal de un usuario
      security: [{bearerAuth: []}]
      parameters:
        - name: usr_id
          in: path
          required: true
          schema:
            type: integer
      responses:
        '200':
          description: Estado de aceptacion

  /api/admin/legal-acceptance:
    get:
      tags: [SuperAdmin]
      summary: Todas las aceptaciones legales (superadmin)
      security: [{bearerAuth: []}]
      parameters:
        - name: emp_id
          in: query
          schema:
            type: integer
      responses:
        '200':
          description: Aceptaciones legales

  # ===== REFERIDOS =====
  /api/referrals/my-code:
    get:
      tags: [Referidos]
      summary: Obtener codigo de referido del usuario
      security: [{bearerAuth: []}]
      responses:
        '200':
          description: Codigo de referido

  /api/referrals/stats:
    get:
      tags: [Referidos]
      summary: Estadisticas de referidos
      security: [{bearerAuth: []}]
      responses:
        '200':
          description: Estadisticas de referidos

  # ===== BILLING LIMITS / USAGE / DASHBOARD =====
  /api/billing/dashboard:
    get:
      tags: [Billing]
      summary: Dashboard de billing del tenant
      security: [{bearerAuth: []}]
      responses:
        '200':
          description: Dashboard

  /api/billing/limits:
    get:
      tags: [Billing]
      summary: Verificar limites del plan
      security: [{bearerAuth: []}]
      responses:
        '200':
          description: Limites

  /api/billing/track:
    post:
      tags: [Billing]
      summary: Registrar uso de metrica
      security: [{bearerAuth: []}]
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                metric:
                  type: string
                count:
                  type: integer
      responses:
        '200':
          description: Uso registrado

  /api/billing/usage:
    get:
      tags: [Billing]
      summary: Uso del mes actual
      security: [{bearerAuth: []}]
      responses:
        '200':
          description: Uso actual

  /api/billing/auto-charge:
    post:
      tags: [Billing]
      summary: Procesar cobros automaticos (cron)
      security: []
      responses:
        '200':
          description: Cobros procesados

  # ===== ADMIN =====
  /api/admin/tenants-usage:
    get:
      tags: [SuperAdmin]
      summary: Uso de todos los tenants (superadmin)
      security: [{bearerAuth: []}]
      responses:
        '200':
          description: Uso de tenants

  /api/admin/legal-acceptance:
    get:
      tags: [SuperAdmin]
      summary: Registros de aceptacion legal global
      security: [{bearerAuth: []}]
      responses:
        '200':
          description: Aceptaciones

components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT
      description: Token JWT del login

  schemas:
    HealthResponse:
      type: object
      properties:
        status:
          type: string
          enum: [OK, DEGRADED]
        timestamp:
          type: string
          format: date-time
        version:
          type: string
        database:
          type: object
          properties:
            engine:
              type: string
            status:
              type: string
            latency:
              type: string
        services:
          type: object
          properties:
            stripe:
              type: string
            mercadopago:
              type: string
            rate_limit:
              type: string

    LoginResponse:
      type: object
      properties:
        success:
          type: boolean
        token:
          type: string
        refresh_token:
          type: string
        expires_in:
          type: integer
          example: 43200
        data:
          type: object
          properties:
            emp_id:
              type: integer
            usuario:
              type: string
            nombre:
              type: string
            rol:
              type: string
            empresa:
              type: string

    PedidoCreate:
      type: object
      required: [pedNumero, clienteNombre, clienteTelefono, destinoDir]
      properties:
        pedNumero:
          type: string
        clienteNombre:
          type: string
        clienteTelefono:
          type: string
        clienteEmail:
          type: string
        destinoDir:
          type: string
        destinoCol:
          type: string
        destinoCiudad:
          type: string
        destinoCp:
          type: string
        pesoKg:
          type: number
        bultos:
          type: integer
        largoCm:
          type: number
        anchoCm:
          type: number
        altoCm:
          type: number
        costoTotal:
          type: number
        formaPago:
          type: string
          enum: [EFECTIVO, TARJETA, TRANSFERENCIA, OXXO]
        prioridad:
          type: string
          enum: [NORMAL, URGENTE, EXPRESS]
        notas:
          type: string

    ChoferCreate:
      type: object
      required: [nombre, telefono]
      properties:
        nombre:
          type: string
        telefono:
          type: string
        email:
          type: string
        licencia:
          type: string

    VehiculoCreate:
      type: object
      required: [placa, tipo]
      properties:
        placa:
          type: string
        tipo:
          type: string
        marca:
          type: string
        modelo:
          type: string
        color:
          type: string
        capacidad_kg:
          type: number

    ClienteCreate:
      type: object
      required: [nombre, telefono]
      properties:
        nombre:
          type: string
        telefono:
          type: string
        email:
          type: string
        direccion:
          type: string

    RegisterRequest:
      type: object
      required: [empresa, usuario]
      properties:
        empresa:
          type: object
          required: [nombre, rfc]
          properties:
            nombre:
              type: string
            rfc:
              type: string
            referral_code:
              type: string
        usuario:
          type: object
          required: [email, password]
          properties:
            email:
              type: string
              format: email
            password:
              type: string
              minLength: 8
              description: Min 8 chars, 1 uppercase, 1 lowercase, 1 digit
            nombre:
              type: string
            telefono:
              type: string
        plan:
          type: string
          enum: [STARTER, PRO, ENTERPRISE]
          default: STARTER
        legal:
          type: object
          properties:
            terminos:
              type: boolean
            privacidad:
              type: boolean
            pagos:
              type: boolean
            deslinde:
              type: boolean
            sla:
              type: boolean
            cookies:
              type: boolean

    UsuarioUpdate:
      type: object
      properties:
        usuario:
          type: string
        nombre:
          type: string
        email:
          type: string
        telefono:
          type: string
        rol:
          type: string
          enum: [admin, operacion, chofer, cliente]
        activo:
          type: string
          enum: ['S', 'N']
        password:
          type: string

    ErrorResponse:
      type: object
      properties:
        success:
          type: boolean
          example: false
        error:
          type: string

    SuccessResponse:
      type: object
      properties:
        success:
          type: boolean
          example: true
        message:
          type: string

    Pedido:
      type: object
      properties:
        PED_ID:
          type: integer
        PED_NUMERO:
          type: string
        PED_CLIENTE_NOMBRE:
          type: string
        PED_CLIENTE_TELEFONO:
          type: string
        PED_DESTINO_DIR:
          type: string
        PED_DESTINO_COL:
          type: string
        PED_DESTINO_CIUDAD:
          type: string
        PED_PESO_KG:
          type: number
        PED_BULTOS:
          type: integer
        PED_COSTO_TOTAL:
          type: number
        PED_FORMA_PAGO:
          type: string
        PED_ESTADO:
          type: string
        PED_PRIORIDAD:
          type: string
        PED_FECHA_PEDIDO:
          type: string
          format: date-time

    Chofer:
      type: object
      properties:
        CHO_ID:
          type: integer
        CHO_NOMBRE:
          type: string
        CHO_APELLIDO:
          type: string
        CHO_TELEFONO:
          type: string
        CHO_EMAIL:
          type: string
        CHO_LICENCIA:
          type: string
        CHO_ESTATUS:
          type: string

    Cliente:
      type: object
      properties:
        CLI_ID:
          type: integer
        CLI_RAZON_SOCIAL:
          type: string
        CLI_RFC:
          type: string
        CLI_CONTACTO:
          type: string
        CLI_TELEFONO:
          type: string
        CLI_EMAIL:
          type: string
        CLI_ESTATUS:
          type: string
